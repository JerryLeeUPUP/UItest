ui 自动化注意点
    ui的文本，图片的显示
    注重交互逻辑的测试
    ui上用户的行为正确性测试

缺陷、难点
    从UI 的角度考虑，一个非预期的缺陷很难被自动化发现，但是手工测试就很容易发现
    ui本身的变化性，导致UI自动化很难达到和手工测试完全相同的覆盖率，单纯的ui自动化测试，很难获取可观的投资回报
    ui 元素控件本身识别的复杂性
    UI测试出现问题的时候，很难完全恢复到吓一条case的场景
    ui测试用例，很多是关于用户交互方面的，用例设计有一定的复杂性

selenium 与 webdriver
    selenium是一个用于web测试的工具，就像是真正的用户在操作浏览器一样，
    webdriver 就是对浏览器提供的操作接口进行封装，使用这套接口，可以操作浏览器

    python 操作 webdriver，webdriver 操作浏览器，浏览器将操作结果反馈给webdriver，webdriver 再反馈给python

HTML
    HTML 元素以开始标签起始
    HTML 元素以结束标签终止
    元素的内容，是开始标签与结束标签之间的内容
    某些HTML元素具有空内容，比如换行标签 <br>
    空元素在开始标签中进行关闭，以开始标签的结束而结束
    大多数HTML元素都有属性
    HTML元素可以设置属性
    属性一般描述于开始标签
    属性经常以键值对的形式出现
